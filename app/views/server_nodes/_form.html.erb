<%= form_for(@server_node) do |f| %>
  <% if @server_node.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@server_node.errors.count, "error") %> prohibited this server_node from being saved:</h2>

      <ul>
      <% @server_node.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
